import { NavigationContainer } from "@react-navigation/native";
import { BottomRoutes } from "./BottomRoutes";

export const Routes = () => {
  return (
    <NavigationContainer>
      <BottomRoutes />
    </NavigationContainer>
  );
};
